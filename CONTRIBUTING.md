# 贡献指南

感谢您对 GitHub Sentinel 项目的关注！我们欢迎各种形式的贡献。

## 如何贡献

### 报告问题

如果您发现了 bug 或有新功能建议，请：

1. 在 [Issues](https://github.com/Elon-Sun/github-sentinel/issues) 中搜索，确保问题未被报告
2. 创建新的 Issue，清晰描述问题或建议
3. 提供必要的复现步骤（如果是 bug）

### 提交代码

1. **Fork 项目**
   ```bash
   git clone https://github.com/Elon-Sun/github-sentinel.git
   cd github-sentinel
   ```

2. **创建分支**
   ```bash
   git checkout -b feature/your-feature-name
   ```

3. **开发并测试**
   ```bash
   # 安装依赖
   pip install -r requirements.txt
   pip install -e .
   
   # 运行测试
   pytest tests/
   ```

4. **提交代码**
   ```bash
   git add .
   git commit -m "feat: 添加新功能描述"
   ```
   
   提交信息格式：
   - `feat:` - 新功能
   - `fix:` - Bug 修复
   - `docs:` - 文档更新
   - `style:` - 代码格式
   - `refactor:` - 重构
   - `test:` - 测试相关
   - `chore:` - 构建/工具相关

5. **推送并创建 Pull Request**
   ```bash
   git push origin feature/your-feature-name
   ```

## 开发规范

### 代码风格

- 遵循 PEP 8 规范
- 使用有意义的变量和函数名
- 添加必要的注释和文档字符串
- 保持函数简洁，单一职责

### 测试

- 为新功能添加单元测试
- 确保所有测试通过
- 保持测试覆盖率在 80% 以上

### 文档

- 更新 README.md（如有必要）
- 更新 CHANGELOG.md
- 添加代码注释

## 项目结构

```
github-sentinel/
├── src/              # 源代码
│   ├── core/        # 核心功能
│   ├── ai/          # AI 相关
│   ├── notifier/    # 通知系统
│   └── storage/     # 数据存储
├── tests/           # 测试用例
├── config/          # 配置文件
└── docs/            # 文档
```

## 行为准则

- 尊重所有贡献者
- 保持友好和专业的交流
- 欢迎新手贡献者

## 获取帮助

如有疑问，可以：
- 创建 Issue 讨论
- 在 Pull Request 中提问

再次感谢您的贡献！🎉
